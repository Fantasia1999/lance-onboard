# Copy this file to `onboard/minio.env`, then adjust the values if needed.

export MINIO_ROOT_USER="ACCESSKEY"
export MINIO_ROOT_PASSWORD="SECRETKEY"
export MINIO_HOST="127.0.0.1"
export MINIO_PORT="9000"
export MINIO_CONSOLE_PORT="9001"
export MINIO_REGION="us-east-1"
export MINIO_BUCKET="lancedb-dev"
export MINIO_ALIAS="pgsty"
export MINIO_ENDPOINT="${MINIO_ENDPOINT:-http://${MINIO_HOST}:${MINIO_PORT}}"
export MINIO_CONSOLE_URL="${MINIO_CONSOLE_URL:-http://${MINIO_HOST}:${MINIO_CONSOLE_PORT}}"

# The S3 smoke test reads standard AWS-style variables, so keep these exports.
export AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID:-$MINIO_ROOT_USER}"
export AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY:-$MINIO_ROOT_PASSWORD}"
export AWS_REGION="${AWS_REGION:-$MINIO_REGION}"
export AWS_ALLOW_HTTP="${AWS_ALLOW_HTTP:-true}"
export AWS_ENDPOINT="${AWS_ENDPOINT:-$MINIO_ENDPOINT}"
export AWS_ENDPOINT_URL="${AWS_ENDPOINT_URL:-$MINIO_ENDPOINT}"

# Optional: pin specific fork releases instead of using the latest GitHub release.
# export PGSTY_MINIO_RELEASE="RELEASE.2026-03-25T00-00-00Z"
# export PGSTY_MC_RELEASE="RELEASE.2026-03-21T00-00-00Z"

# Optional: override where local runtime artifacts are stored.
# export LOCAL_ROOT="/absolute/path/to/local"
